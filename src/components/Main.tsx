import React, { useState } from 'react';

const MainView = (): JSX.Element => {  
   
   return (
       <div className="main">           
            <input placeholder='Search... Not available in demo :-)'></input>          
       </div>
   );
};

export default MainView;